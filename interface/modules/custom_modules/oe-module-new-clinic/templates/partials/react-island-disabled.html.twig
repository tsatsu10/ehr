{# Shown when a React island flag is OFF — legacy jQuery desks were removed in the w50react cutover. #}
<div class="nc-callout nc-callout-warn nc-mb-0" role="alert">
    {{ '%s requires the React island. Enable it in Clinic Setup → React migration flags.'|xlt(desk_label) }}
</div>
