{% extends 'base.html.twig' %}

{% block page_subtitle %}
<div class="oe-nc-page-heading__subtitle" id="nc-doctor-date"></div>
{% endblock %}

{% block page_heading_actions %}
{% if enable_multi_doctor_filters %}
<select class="form-control form-control-sm" style="max-width:120px;" id="nc-doctor-scope">
    <option value="me">{{ 'Me'|xlt }}</option>
    <option value="all">{{ 'All'|xlt }}</option>
</select>
{% endif %}
<button type="button" class="btn btn-outline-primary btn-sm" id="nc-doctor-refresh" aria-label="{{ 'Refresh'|xlt }}">
    <i class="fa fa-refresh" aria-hidden="true"></i>
</button>
<span class="text-muted small" id="nc-doctor-updated"></span>
{% endblock %}

{% block content %}
{% if enable_react_doctor_desk|default(true) %}
<div
    data-island="doctor-desk"
    data-props="{{ {
        ajaxUrl:               ajax_url,
        csrfToken:             csrf_token,
        facilityId:            desk_facility_id|default(0)|number_format(0, '', ''),
        visitBoardUrl:         visit_board_url,
        multiDoctorFilters:    enable_multi_doctor_filters ? true : false,
        sharedDeviceWarning:   enable_shared_device_session_warning ? true : false,
        labPanelOrderEnabled:  lab_panel_order_enabled ? true : false
    }|json_encode|e('html_attr') }}"
></div>
{% else %}
{% include 'partials/react-island-disabled.html.twig' with { desk_label: 'Doctor Desk' } %}
{% endif %}
{% endblock %}

{% block scripts %}
{% if enable_react_doctor_desk|default(true) %}
<link rel="stylesheet" href="{{ assets_url }}/modern/doctor-desk.css?v={{ asset_version }}">
<script type="module" src="{{ assets_url }}/modern/doctor-desk.js?v={{ asset_version }}"></script>
{% endif %}
{% endblock %}
